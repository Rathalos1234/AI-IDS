[pytest]
addopts = -q
pythonpath = .
markers =
    unit: fast, isolated tests
    integration: multi-module flow tests
    perf: performance smoke tests with budgets
    scapy_required: tests needing scapy (may be skipped)
