[pytest]
addopts = -q
testpaths = tests
markers =
    unit: fast, isolated tests
    integration: stitched flows across modules
    perf: performance and resource tests
    scapy_required: requires scapy to be installed and available